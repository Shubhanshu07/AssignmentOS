## Implementation of Reader_writter(Reader priority) problem  using semaphore

Instruction to execute the program using command line
```
  gcc reader_writers.c -o thread -lpthread
  ./thread
```
*For two reader and two writers*
